package app.enums;

public enum AuditEventTypes {
    ACCESS_ALLOWED,
    ACCESS_DENIED,

    AUDIT_FAILURE,

    DELETE_ERROR,
    ENTITY_NOT_EXISTS,
    INVALID_STATE,
    SAVING_ERROR,
    VALIDATION_ERROR,

    SUCCESSFUL
}
